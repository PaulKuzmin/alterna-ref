import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.alternadv.ved2023',
  appName: 'alterna-ref',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
